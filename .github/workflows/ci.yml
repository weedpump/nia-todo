name: CI

# Automatic: tests only, on PRs and merges to main.
# Builds are never automatic - only via manual dispatch (this file), or via
# the tag-triggered release workflow (release.yml).
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      build_windows:
        description: "Build Windows installer"
        type: boolean
        default: false
      build_android:
        description: "Build Android APK"
        type: boolean
        default: false
      build_debian_desktop:
        description: "Build Debian desktop .deb"
        type: boolean
        default: false
      build_docker:
        description: "Build Docker image (embeds native apps: freshly built ones above if selected, otherwise the latest published GitHub release's apps)"
        type: boolean
        default: false
      build_server_deb:
        description: "Build the full server .deb installer (same embedding rule as Docker)"
        type: boolean
        default: false
      push_docker:
        description: "Push the built Docker image to GHCR as :dev-<sha>"
        type: boolean
        default: false

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install backend dependencies
        run: pip install -r packaging/requirements.txt

      - name: Install frontend/native toolchain dependencies
        run: npm ci

      - name: Run test suite
        run: ./scripts/test_all.sh

  build-windows:
    if: github.event_name == 'workflow_dispatch' && inputs.build_windows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "22", cache: "npm" }
      - run: npm ci
      - uses: dtolnay/rust-toolchain@stable
        with: { targets: x86_64-pc-windows-msvc }
      - run: |
          rustup target add x86_64-pc-windows-msvc
          cargo install cargo-xwin
      - run: python3 scripts/prepare_tauri_frontend_dist.py
      - run: npm run tauri -- build --target x86_64-pc-windows-msvc --runner cargo-xwin
      - name: Rename to a fixed artifact name
        run: |
          mkdir -p dist/ci-windows
          cp src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe dist/ci-windows/nia-todo-ci-windows-x64-setup.exe
      - uses: actions/upload-artifact@v4
        with:
          name: native-windows
          retention-days: 7
          path: dist/ci-windows/*.exe

  build-android:
    if: github.event_name == 'workflow_dispatch' && inputs.build_android
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "22", cache: "npm" }
      - uses: actions/setup-python@v5
        with: { python-version: "3.13" }
      - run: npm ci
      - uses: dtolnay/rust-toolchain@stable
      - uses: android-actions/setup-android@v3
      - run: sdkmanager --install "ndk;27.0.12077973" "build-tools;35.0.0"
      - name: Restore Android release keystore
        run: |
          mkdir -p secrets
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > secrets/nia-todo-android-release.keystore
          printf '%s' "${{ secrets.ANDROID_KEYSTORE_PASS }}" > secrets/nia-todo-android-release.pass
      - run: python3 scripts/prepare_tauri_frontend_dist.py
      - run: npm run tauri -- android build --target aarch64 --apk --ci
      - name: Align and sign APK
        env:
          NIA_TODO_SECRETS_DIR: ${{ github.workspace }}/secrets
        run: |
          BUILT_APK="src-tauri/gen/android/app/build/outputs/apk/universal/release/app-universal-release-unsigned.apk"
          ALIGNED_APK="/tmp/nia-todo-ci-android-arm64-aligned.apk"
          "${ANDROID_HOME}/build-tools/35.0.0/zipalign" -p -f 4 "${BUILT_APK}" "${ALIGNED_APK}"
          mkdir -p dist/ci-android
          export ANDROID_KEYSTORE_PASS=$(tr -d '\r\n' < secrets/nia-todo-android-release.pass)
          "${ANDROID_HOME}/build-tools/35.0.0/apksigner" sign \
            --ks secrets/nia-todo-android-release.keystore \
            --ks-key-alias nia-todo-android-release \
            --ks-pass env:ANDROID_KEYSTORE_PASS --key-pass env:ANDROID_KEYSTORE_PASS \
            --out dist/ci-android/nia-todo-android-arm64.apk "${ALIGNED_APK}"
          unset ANDROID_KEYSTORE_PASS
      - if: always()
        run: rm -rf secrets
      - uses: actions/upload-artifact@v4
        with:
          name: native-android
          retention-days: 7
          path: dist/ci-android/*.apk

  build-debian-desktop:
    if: github.event_name == 'workflow_dispatch' && inputs.build_debian_desktop
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "22", cache: "npm" }
      - uses: actions/setup-python@v5
        with: { python-version: "3.13" }
      - run: npm ci
      - run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwebkit2gtk-4.1-dev libjavascriptcoregtk-4.1-dev libsoup-3.0-dev \
            build-essential curl wget file libssl-dev libgtk-3-dev librsvg2-dev dpkg-dev
      - uses: dtolnay/rust-toolchain@stable
      - run: python3 scripts/prepare_tauri_frontend_dist.py
      - run: npm run tauri -- build --bundles deb
      - name: Repack with the correct package name
        run: |
          mkdir -p dist/ci-debian-desktop
          BUILT="$(scripts/release/repack-native-debian-deb.sh src-tauri/target/release/bundle/deb/nia-todo_*_amd64.deb)"
          cp "${BUILT}" dist/ci-debian-desktop/nia-todo-desktop-amd64.deb
      - uses: actions/upload-artifact@v4
        with:
          name: native-debian-desktop
          retention-days: 7
          path: dist/ci-debian-desktop/*.deb

  # Docker/server-.deb always embed native apps: freshly built ones from the
  # jobs above if selected, otherwise the latest published GitHub release's
  # apps (fetched automatically). There is no "no apps embedded" mode.
  package:
    if: |
      github.event_name == 'workflow_dispatch' &&
      always() &&
      (inputs.build_docker || inputs.build_server_deb)
    needs: [build-windows, build-android, build-debian-desktop]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.13" }

      - name: Download freshly built native artifacts (if any were built)
        uses: actions/download-artifact@v4
        with:
          path: dist/ci-native
          pattern: native-*
        continue-on-error: true

      - name: Collect native apps for embedding
        id: apps
        run: |
          mkdir -p dist/native-embed
          have_windows=0; have_android=0; have_debian=0
          [ "${{ needs.build-windows.result }}" = "success" ] && cp dist/ci-native/native-windows/nia-todo-ci-windows-x64-setup.exe dist/native-embed/ && have_windows=1
          [ "${{ needs.build-android.result }}" = "success" ] && cp dist/ci-native/native-android/nia-todo-android-arm64.apk dist/native-embed/nia-todo-ci-android-arm64.apk && have_android=1
          [ "${{ needs.build-debian-desktop.result }}" = "success" ] && cp dist/ci-native/native-debian-desktop/nia-todo-desktop-amd64.deb dist/native-embed/nia-todo-ci-debian-amd64.deb && have_debian=1

          if [ "${have_windows}" = "1" ] && [ "${have_android}" = "1" ] && [ "${have_debian}" = "1" ]; then
            echo "source=fresh" >> "$GITHUB_OUTPUT"
            echo "native_app_version=0.0.0-ci" >> "$GITHUB_OUTPUT"
          else
            echo "No (complete) fresh native builds selected; fetching the latest published release's apps instead."
            scripts/release/fetch-latest-native-artifacts.sh \
              --github-repo "${{ github.repository }}" \
              --output-dir dist/native-embed
            source dist/native-embed/native-app-version.env
            echo "source=latest-release" >> "$GITHUB_OUTPUT"
            echo "native_app_version=${native_app_version}" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with: { node-version: "22", cache: "npm" }
      - run: npm ci

      - name: Build Docker image
        if: inputs.build_docker
        run: |
          scripts/release/build-docker.sh 0.0.0 \
            --windows-installer dist/native-embed/nia-todo-ci-windows-x64-setup.exe \
            --android-apk dist/native-embed/nia-todo-ci-android-arm64.apk \
            --debian-deb dist/native-embed/nia-todo-ci-debian-amd64.deb \
            --native-app-version "${{ steps.apps.outputs.native_app_version }}" \
            --tag "nia-todo:ci-check" \
            --output "dist/docker/ci-${{ github.sha }}" \
            --force

      - name: Push Docker image to GHCR
        if: inputs.build_docker && inputs.push_docker
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          docker tag "nia-todo:ci-check" "ghcr.io/${{ github.repository_owner }}/nia-todo:dev-${{ github.sha }}"
          docker push "ghcr.io/${{ github.repository_owner }}/nia-todo:dev-${{ github.sha }}"

      - name: Build server .deb installer
        if: inputs.build_server_deb
        run: |
          scripts/release/build-full-bundle.sh 0.0.0 \
            --windows-installer dist/native-embed/nia-todo-ci-windows-x64-setup.exe \
            --android-apk dist/native-embed/nia-todo-ci-android-arm64.apk \
            --debian-deb dist/native-embed/nia-todo-ci-debian-amd64.deb \
            --native-app-version "${{ steps.apps.outputs.native_app_version }}" \
            --output-dir "dist/release/ci-${{ github.sha }}" \
            --force

      - uses: actions/upload-artifact@v4
        if: inputs.build_server_deb
        with:
          name: server-deb
          retention-days: 7
          path: dist/release/ci-${{ github.sha }}/*.deb
