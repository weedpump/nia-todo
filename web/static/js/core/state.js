export const state = {
  todos: [],
  projects: [],
  sections: [],
  currentFilter: 'all',
  currentProjectId: null,
  dragSrcTodoId: null,
  dragSrcSectionId: null,
  db: null,
  dbReady: null,
  appInitialized: false,
  syncInProgress: false,
  swRegistration: null,
  updateAvailable: false,
  hideDone: localStorage.getItem('nia-hide-done') !== 'false',
  sortMode: localStorage.getItem('nia-sort') || 'priority',
  undoAction: null,
  undoTimer: null,
  pendingUndoBatch: null,
  currentUser: null,
  ws: null,
  wsState: 'disconnected',
  reconnectAttempts: 0,
  pingInterval: null,
  reconnectTimer: null,
  wsIntentionalClose: false,
  pushSubscription: null,
  expandedProjects: new Set(),
};

export const WS = {
  MAX_RECONNECT_ATTEMPTS: 10,
  BASE_RECONNECT_DELAY: 1000,
};
