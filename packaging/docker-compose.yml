services:
  nia-todo:
    image: nia-todo:latest
    build: .
    ports:
      - "8753:8753"
    environment:
      NIA_TODO_HOST: 0.0.0.0
      NIA_TODO_PORT: 8753
      NIA_TODO_DB: nia-todo.db
    volumes:
      - nia-todo-data:/app/api/data

volumes:
  nia-todo-data:
